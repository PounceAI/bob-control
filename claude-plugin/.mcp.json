{
  "mcpServers": {
    "bob-tasks": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/server.mjs"],
      "env": {
        "BOB_TASKS_DB": "${CLAUDE_PROJECT_DIR}/data/tasks.db"
      }
    }
  }
}
